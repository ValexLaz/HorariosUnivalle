import ParentController from "../../common/ParentController";

class TimeAvailsController extends  ParentController{
    constructor(recordService) {
        super(recordService);
    }
}
export  default TimeAvailsController;