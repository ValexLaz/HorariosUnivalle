import ParentController from "../../common/ParentController";

class BuildingController extends ParentController{
    constructor(recordService) {
        super(recordService);
    }
}
export default BuildingController;